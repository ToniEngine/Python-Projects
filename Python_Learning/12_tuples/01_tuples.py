# tuples = This is another data type in python
# tuples are similar to list but unlike list they are immutable


numbers = (1,2,3)
print(numbers[0]) # We can access items in a tuple using the index of the number

# We cannot change, modify or mutate a tuple